name: Deploy
on:
 push:
   branches:
     - prod
jobs:
 deploy:
   runs-on: ubuntu-latest
   steps:
     - name: SSH Deploy
       uses: appleboy/ssh-action@v1
       with:
         host: ########
         username: #######
         password: ######
         script: |
           cd /home/globify
           ./deploy.sh
